<% provide(:title, 'Base edit') %>
<% provide(:class_text, 'base--edit') %>
<% provide(:button_text, '更新') %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <button type="button" class="btn btn-block btn-default btn-<%= yield(:class_text) %>">
      <%= link_to "キャンセル", bases_path %>
    </button>
  </div>
</div>